/**
 * Crie uma função que remova elementos duplicados de um array.
 */

function removaElementos(numerosQuaisquer: number) {

    const remover = [...new Set()]

    return remover;
}

let valoresAleatorios: any = [5, 8, 15, 6, 4, 45, 32, 78, 65];

console.log(removaElementos(valoresAleatorios));
